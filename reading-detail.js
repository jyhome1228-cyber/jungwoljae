(() => {
  // Deprecated generic enhancer. It used keyword matching (for example 水 ->
  // research/writing) across every service, which could put work-oriented
  // advice inside relationship reports. Service-specific result copy now owns
  // its examples, so this file intentionally does nothing.
})();
